import React from 'react'

type Props = {
    amount: number;
}

// Get amount of questions to use in single exam

export default function AmountOfQuestions({}: Props) {
  return (
    <div>AmountOfQuestions</div>
  )
}